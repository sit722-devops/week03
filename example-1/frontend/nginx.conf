# week03/example-1/frontend/nginx.conf
# Simple Nginx configuration to serve index.html

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
